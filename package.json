{
  "name": "ct-cloud-cron-service",
  "version": "0.0.1",
  "description": "Server timer t send message to the API every 24 hours",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cron": "^1.1.1",
    "request": "^2.79.0",
    "winston": "^2.3.0"
  }
}
